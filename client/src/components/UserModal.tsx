import { Box, Modal } from "@material-ui/core";
import React from "react";

const UserModal = () => {
  return (
    <Modal open={true}>
      <Box></Box>
    </Modal>
  );
};

export default UserModal;
